#!/bin/bash
# This script is located inside the 'backend' directory,
# so we can run the server directly.
# Make sure your virtual environment is activated before running this script.
python server.py